# fastcontain

A faster alternative to Python's string._\_contain__ using string hashing.

Current version imlemented in Cython still not fast enough to beat the native C implementation of Python's string contain.
